version: "3.9"
services:
  api:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ./app:/app
    ports:
      - "8000:8000"
    command: >
      bash -c "pip install fastapi uvicorn watchfiles -q &&
               uvicorn main:app --host 0.0.0.0 --port 8000 --reload"